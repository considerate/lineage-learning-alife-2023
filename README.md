# Lineage Learning: Combining Evolution with Gradient Descent for Agent-Environment Interaction Tasks

## Installing

```console
$ julia --project -e 'import Pkg; Pkg.instantiate();'
```

## Running

```console
$ julia --threads=auto --project src/Lineages.jl
```
